<p id="notice"><%= notice %></p>
 <section id="portfolio">
    <div class="container wow fadeInUp">
      <div class="row">
        <div class="col-md-12">
          <h3 class="section-title">Cartel Oficial</h3>
          <div class="section-title-divider"></div>
          <p class="section-description">Los eventos realizados en el recinto ferial, son eventos con calidad, ofreciendo un ambiente familiar.</p>
        </div>
      </div>
      
      <div class="row">
         <% @posters.each do |poster| %>
        <div class="col-md-3">
          <a class="portfolio-item"  href="">            
            <div class="pic" ><%= image_tag(poster.fot) %>
            <div class="details">
              <h4><%= poster.name %></h4>
              <p>Se presentará el dia:</p>
              <span><%= poster.fech %></span><br>
              <p> con un costo de:</p>
              <p><%= poster.cost %></p>
            </div>
            </div>
          </a>
          <% if user_signed_in? %>
          <%= link_to 'Modificar', edit_poster_path(poster), :class => 'btn btn-success' %>
        <%= link_to 'Eliminar', poster, method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn btn-danger'  %>
         <% end %>
        </div>
        
          <% end %>
          <br>
      </div>
    </div>
  </section>
    <br>

  <% if user_signed_in? %>
    <%= link_to 'Agregar Artista', new_poster_path, :class => 'btn btn-info' %>
        <% end %>
    <br>




